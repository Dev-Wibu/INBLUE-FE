fix(mentor-review): handle legacy empty-detail + retry CTA + missing fields

- ApplicationMentorReviewPage: distinguish loading vs detail-missing vs
  recoverable cases; show "Booking system hasn't initialized" empty state
  instead of infinite spinner for legacy applications (detail not yet
  created by backend moveToNextRound); add retry button for recoverable case
- ApplicationHistoryPage: defensive detail lookup fallback when current
  round detail is missing
- MentorSessionRoomPage: surface sessionId + bookingId fetch errors and
  prevent action when session data unavailable
- HrMentorReviewApprovalPage: handle empty finalScore/hrScore values
- mentor-review.manager: add missing helper for retry + tolerate nulls
- useSession: expose sessionId/bookingId availability flags

Co-Authored-By: Claude <noreply@anthropic.com>